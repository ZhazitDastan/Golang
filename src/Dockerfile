# Используем образ Golang в качестве базового образа
FROM golang:latest

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем содержимое текущего каталога внутрь контейнера
COPY . .

# Компилируем приложение
RUN go build -o app .

# Запускаем приложение по умолчанию при запуске контейнера
CMD ["./app"]
